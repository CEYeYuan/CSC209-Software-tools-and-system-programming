#!/bin/bash  
#the first line defines the shell to run the program
export PATH="$PATH:~/Users/ye/gitRepo/csc209/lecture"
echo "Get started"
export COLOR="yellow"
#when define a new var, we cannot have spaces around the equal sign
#the var only exists when this shell script is running even if we use export
echo "$COLOR"